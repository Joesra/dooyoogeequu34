# Disabled temporary: Duplicate/unfinished exercise file
# This module (oefen.py) contained a mix of SQL and Python and used
# different DATABASE keys (e.g. "HOST" instead of "host"). That
# caused import/syntax errors and inconsistent DB behavior for routes.
# To avoid accidental imports and runtime errors we disabled the file.
# If you need the functionality, please move the working parts into
# `app/events/routes.py` and ensure consistent DATABASE usage.

      
